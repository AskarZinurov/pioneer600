require 'spec_helper'

describe Pioneer600 do
  it 'has a version number' do
    expect(Pioneer600::VERSION).not_to be nil
  end

  it 'does something useful' do
    expect(false).to eq(true)
  end
end
